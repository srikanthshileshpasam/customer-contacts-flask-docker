# Used only when you run the app outside Docker
DB_HOST=localhost
DB_NAME=contacts
DB_USER=appuser
DB_PASS=mypassword123
DB_PORT=5433          # ← note the new host port
FLASK_SECRET_KEY=change-this-to-a-long-random-string-in-production
PORT=5000